上节课作业讲解：
输入两个数，输出较大数
输入一个数，输出绝对值（不使用abs方法，自己实现）

list
初始化
长度
下标
插入
删除


tuple（元组）
tuple和list非常类似，但是tuple一旦初始化就不能修改。

特别注意：只定义一个元素时的写法。
t = (1,)



顺序结构
分支结构
循环结构

Python的循环有两种：
for...in循环
while循环


continue、break

debug（程序单步执行）

print(x, end="")    end="" 可使输出不换行。

求1~10累加
打印99乘法表

作业：
求水仙花数（3位数，各位数的立方和等于本身，例： 153=1^3 + 5^5 + 3^3）
用*打印图形：三角形
猜拳游戏
import random
random.randint(0,2)